discovery:
  task: text-generation
  sort: trending
  max_size_gb: 75
  limit: 50
  include_patterns: ["*instruct*", "*chat*"]
  exclude_patterns: []
  exclude_models: []
backends: [vllm, llamacpp]
load:
  max_models_per_day: 5
  iterations_per_conversation: 5
  per_backend_timeout_s: 3600
